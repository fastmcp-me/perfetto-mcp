[project]
name = "perfetto-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.12.1",
    "perfetto>=0.14.0",
    "protobuf<5",
]
